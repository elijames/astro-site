---
interface Props {
    title: string;
    description: string;
    image: string;
}

const { title, description, image } = Astro.props;
---

<div>
    <h1>{title}</h1>
    <p>{description}</p>
    <img src={image} alt={title} />
</div>